{
    "version": "0.2.0",
    "configurations": [
    go
        {
            "name": "Launch Server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/server.go"
        }
    ]
}
